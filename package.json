{
  "name": "iksn-rebuild",
  "version": "2.0.3",
  "private": true,
  "description": "IKSN fictional institutional prototype with Railway proxy handling, random server-logged OTP, resend OTP UI, consented location, messaging and camera verification UI.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0"
  }
}
